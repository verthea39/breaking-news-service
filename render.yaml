services:
  - type: web
    name: breaking-news-service
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    region: singapore # or oregon, frankfurt, etc.
    envVars:
      - key: PORT
        value: 3000
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
